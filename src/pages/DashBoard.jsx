
import PropTypes from 'prop-types';

const DashBoard = props => {
    return (
        <div>
            <h1>dash board</h1>
        </div>
    );
};

DashBoard.propTypes = {
    
};

export default DashBoard;