
import PropTypes from 'prop-types';

const Statistics = props => {
    return (
        <div>
            <h1>Statistics</h1>
        </div>
    );
};

Statistics.propTypes = {
    
};

export default Statistics;