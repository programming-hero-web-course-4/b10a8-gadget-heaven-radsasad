
import PropTypes from 'prop-types';

const CartItem = () => {
    return (
        <div>
            <h1>Cart item</h1>
        </div>
    );
};

CartItem.propTypes = {
    
};

export default CartItem;