import PropTypes from 'prop-types';

const AddCartList = () => {
    return (
        <div>
            <h1>add cart list</h1>
        </div>
    );
};

AddCartList.propTypes = {
    
};

export default AddCartList;